<div class="conents row">
  <div class="container">
    <h3>投稿する</h3>
    <%= form_with(model: @tweet, lpcal: true) do |form| %>
      <%= form.text_field :name, placeholder: "Nickname" %>
      <%= form.text_field :image, placeholder: "Image Url" %>
      <%= form.text_area :texy, placeholder: "text", rows: "10" %>
      <%= form.sumbit "SEND" %>
    <% end %>
  </div>
</div>